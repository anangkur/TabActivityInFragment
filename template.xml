<template format="4"
        revision="1"
        name="Tab Layout in Fragment Activity"
        description="Creates a new Tab activity within fragment classes">

    <category value="Activity"/>

    <parameter
        id="mainActivityClass"
        name="Activity Name"
        type="string"
        constraints="class|unique|nonempty"
        suggest="${layoutToActivity(mainLayout)}"
        default="MainActivity"
        help="The name of the activity class to create" />

    <parameter
        id="mainActivityLayout"
        name="Layout Name"
        type="string"
        constraints="layout|unique|nonempty"
        suggest="${activityToLayout(mainActivityName)}"
        default="main_activity"
        help="The name of the layout to create for the activity" />

    <parameter
        id="mainFragmentClass"
        name="Fragment Name"
        type="string"
        constraints="class|unique|nonempty"
        default="MainFragment"
        help="The name of the fragment class to create" />

    <parameter
        id="mainFragmentLayout"
        name="Fragment Layout Name"
        type="string"
        constraints="layout|unique|nonempty"
        default="main_fragment"
        help="The name of the layout to create for the activity" />

    <parameter
        id="tabFragmentClass"
        name="Fragment Name"
        type="string"
        constraints="class|unique|nonempty"
        default="MainTabFragment"
        help="The name of the fragment class to create" />

    <parameter
        id="tabFragmentLayout"
        name="Fragment Layout Name"
        type="string"
        constraints="layout|unique|nonempty"
        default="main_tab_fragment"
        help="The name of the layout to create for the tab layout" />

    <parameter
        id="packageName"
        name="Package name"
        type="string"
        constraints="package"
        default="com.mycompany.myapp" />

    <execute file="recipe.xml.ftl" />

</template>